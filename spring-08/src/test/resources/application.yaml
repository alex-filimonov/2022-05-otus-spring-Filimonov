spring:
  data:
    mongodb:
      uri: mongodb://127.0.0.1:27017?retryWrites=true


mongock:
  #runner-type: "ApplicationRunner" # default
  runner-type: "InitializingBean"
  change-logs-scan-package:
    - ru.otus.spring.spring08.test.mongock.changelog